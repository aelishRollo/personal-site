import { Link } from 'react-router-dom';

export default function Navbar() {
    return (
        <nav className='App-header'>
            <ul>
            {/*<li><Link to="./components/Homepage">Home</Link></li>*/}
            {/*something is wrong with that code above. The component renders fine without it, but it gets fucked up*/}
               
            </ul>
        </nav>
    );
}
